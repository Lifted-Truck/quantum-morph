# INDEX — quantum-morph knowledge map

Tags: `m4l-lom` (Live Object Model gotchas) · `max-js` (Max js/v8 quirks) ·
`morph-engine` (QM-0 math lessons) · `device-ux` (jsui/pattr/presentation).

- L0003 · morph-engine — QM-0 §10.4's "not exactly on a boundary" is load-bearing; a naive position grid fails on a correct engine. Skip ties, don't loosen the assertion.
- L0002 · m4l-lom — LOM write saturation is real and field-proven (J74 Morph ships a throttle); QM-1 §5 write budget is a hard constraint, expect a user-facing rate control.
- L0001 · morph-engine — prototype's noise-blending coupling is superseded; use QM-0 §5 mask formulation, never port blending.
