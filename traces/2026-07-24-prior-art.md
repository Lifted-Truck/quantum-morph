# prior-art — P0 prior-art landscape sweep

- **Queue item:** ROADMAP Q-001 (open → done).
- **Why:** kit Decision 30 requires the prior-art bookend *before* the design
  is committed. The design here is already written (QM-0 is normative), so the
  sweep's real job was to find anything that would force a spec revision before
  P1 builds against it. It did not: the closest patent claims proportional
  crossfade of switches, which is the mechanism QM-0 explicitly refuses.
- **Evidence consulted:** `docs/specs/QM-0-core-engine-spec.md` §1-§3 (purity
  and selection law), `QM-1` §1/§5 (MIDI-effect rationale, write budget),
  `QM-2` §1 (the discrete-parameters premise). External: US10770048B2 (fetched
  and claim-read directly), US4352311, Prophet VS/SY22/Wavestation histories,
  J74 Morph FAQ, maxforlive device listings, Huijben et al. arXiv:2110.01515,
  Le Vaillant TASLP 2024 + SPINVAE. All cited in `docs/prior-art.md`.
- **Alternatives rejected:**
  - *Agent fan-out for the sweep* (as the ROADMAP P0 text suggested) —
    rejected: not requested by the human this session, and a ~10-search
    single-thread sweep covered the ground. The gaps left are the kind a
    formal FTO search closes, not the kind more agents close (§7 of the doc).
  - *Declaring the IP question settled* — rejected: US10770048B2 is active to
    ~2038 and does reach discrete elements. Recorded as a flag for P3 with an
    explicit "not counsel" disclaimer rather than resolved here.
- **Verify:** `fast` exit 0, git hash recorded in `.harness/last-verify.json`
  at commit time (see the commit that carries this trace).
- **Open questions:** (1) No formal patent-family/FTO search — deferred to P3
  per `docs/prior-art.md` §7. (2) "QUANTUM MORPH" as a shipping name overlaps
  the Waldorf Quantum goods class; needs a trademark check before release, not
  before P1. (3) ROADMAP P0's original device examples ("NestUp, Sting") were
  a scaffold-time guess and were wrong — corrected in ROADMAP to the devices
  actually found. Prior traces are append-only; this entry is the correction
  record for `traces/2026-07-24-scaffold.md`.
