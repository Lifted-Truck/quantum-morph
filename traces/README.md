# traces/

One file per merged change (see the provenance skill in `.claude/skills/`):
what changed, why, evidence consulted, verify result + git hash. Written by
the lead session as part of "done" — `./verify full` green alone is not done.
