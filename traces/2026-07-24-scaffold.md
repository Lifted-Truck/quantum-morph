# scaffold — spin-up: survey, harness, specs, oracle, remote sync

- **Queue item:** unqueued: the spin-up itself (ONBOARDING Part 2, 8 steps);
  it is what creates the queue.
- **Why:** Establish quantum-morph as a harnessed project before any engine
  code exists, so the first line of engine code lands against a green oracle,
  a normative spec, and a written architecture decision rather than after them.
- **Evidence consulted:** `~/Documents/Claude/autonomous/ONBOARDING.md:141-171`
  (the eight steps), `autonomous/kit/README.md` (9-question survey, Decisions
  28/30/31), `autonomous/harness/{CLAUDE.md,verify,.claude/}` (copied),
  `autonomous/kit/templates/ci.github.yml`,
  `autonomous/loops/knowledge-loop/integrate-knowledge-loop.prompt.md`,
  `~/Documents/Claude/distillery/project.manifest.json` (manifest shape).
  Seed material: the three QM specs (from `files.zip`) and the lab prototype.
- **Alternatives rejected:**
  - *Composite spin-up* — rejected: one deployable, one module. The QM-0/QM-1
    split is spec-vs-implementation, not a contract-bound module fleet. Revisit
    at QM-2 (multiple instrument integrations), recorded as ROADMAP P4.
  - *Rung 2 (subagents/verifier)* — offered in the menu, human chose 1. No
    parallelizable verifiable work at two artifacts.
  - *Python for the engine* — rejected: the engine must run inside Max's `v8`
    object, so JS is forced; the same source then serves Node tests and the
    device with no port (DECISIONS D-002).
  - *Aspirational red oracle* (asserting engine tests that don't exist) —
    rejected per ONBOARDING step 2: green-from-day-zero, honestly scoped.
- **Verify:** `fast` exit 0, git `dff0a84`; CI run `ci` on `main` = success
  (remote oracle mirrors local).
- **Open questions:** manifest is PROVISIONAL — the P0 human ratification has
  not happened. Ecosystem-track registration in `autonomous` is unfiled
  (governor/resident-only ROADMAP edit; not attempted from here).
